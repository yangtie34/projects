package com.jhnu.framework.data.source;

public class DBContextHolder{
	/*public static final String DATA_SOURCE_BASE = "ds-base";
	public static final String DATA_SOURCE_LOG = "ds-log";
	
	
	private static final ThreadLocal<String> contextHolder = new ThreadLocal<String>();
	
	public static void setDBType(String dbType) {
		if(!dbType.equals(getDBType())){
			contextHolder.set(dbType);
		}
	}
	
	public static String getDBType() {
		return contextHolder.get();
	}
	
	public static void clearDBType() {
		contextHolder.remove();
	} */
	
	//DBContextHolder.setDBType(DBContextHolder.DATA_SOURCE_BASE);
}